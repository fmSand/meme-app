<nav class="navbar navbar-expand-lg position-fixed w-100 fixed-top shadow-sm">
    <div class="container-fluid">

        <a class="navbar-brand" href="/memes">
            <img src="/images/logo.png" alt="Central Logo" class="d-inline-block align-text-top me-2" />
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a 
                        class="nav-link <%= path === '/' ? 'active' : '' %>" 
                        href="/"
                        <%= path === '/' ? 'aria-current="page"' : '' %>>
                        Home
                    </a>
                </li>
                <li class="nav-item">
                    <a 
                        class="nav-link <%= path === '/memes' ? 'active' : '' %>" 
                        href="/memes"
                        <%= path === '/memes' ? 'aria-current="page"' : '' %>>
                        Memes
                    </a>
                </li>

                <% if (isAuthenticated) { %>
                    <li class="nav-item">
                        <a class="nav-link auth-button" href="/auth/logout">
                            <i class="bi bi-box-arrow-right me-1"></i>Log out
                        </a>
                    </li>
                    <li class="nav-item">
                        <span class="nav-link user-status">
                            <i class="bi bi-person-circle me-1"></i><%= user.username %>
                        </span>
                    </li>

                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link auth-button" href="/auth/login">
                                <i class="bi bi-box-arrow-in-right me-1"></i>Login
                            </a>
                        </li>
                        <li class="nav-item">
                            <span class="nav-link user-status">
                                <i class="bi bi-person-circle me-1"></i>Guest
                            </span>
                        </li>
                        <% } %>
            </ul>
        </div>
    </div>
</nav>